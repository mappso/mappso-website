import React from "react";
import { ResumeSectionModel } from "../models/resumeModel";

interface Props {}

const BeamHeroSection: ResumeSectionModel<Props> = (props) => {
    return (
        <div>
            <h1>Beam hero</h1>
        </div>
    );
};

export default BeamHeroSection;
